import { findId } from './utils.js';
import { renderTableRow } from './render-table-rows.js';
import { violins } from '../data';
import { cart } from './cart-data.js';

const table = document.querySelector('table');

let total = 0;

for (let item of cart) {
    c
}